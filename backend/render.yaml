services:
  - type: web
    name: truematch-backend
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: "YOUR_DATABASE_URL"
      - key: PORT
        value: "5000"
